#include <iostream>
#include <string>
#include <vector>

using namespace std;

class A {
    int a[100][1000];
};

int main() {
    auto aa = new A();
    int N, Q;
    string s;
    scanf("%d", &N);
    scanf("%d", &Q);
    while (Q--) {
    }
}
